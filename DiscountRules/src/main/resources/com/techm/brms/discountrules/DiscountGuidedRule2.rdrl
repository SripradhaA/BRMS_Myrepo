package com.techm.brms.discountrules;

import java.lang.Number;

rule "DiscountGuidedRule2"
	dialect "mvel"
	when
		OrderDO( amount > 0 , amount < 500 )
	then
		double f=p.getAmount()*0.05d;
		OrderDO fact0 = new OrderDO();
		fact0.setDiscount( f );
		insert( fact0 );
end
